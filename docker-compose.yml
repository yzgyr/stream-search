services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3333:3000" # Map port 3000 on the host to port 3000 in the container
    environment:
      NODE_ENV: production
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped